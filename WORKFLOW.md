# 信息瓶颈循环网络与全局工作空间"点燃"机制研究
## 实验工作流文档 v1.1

> **研究目标**：通过构建基于"信息瓶颈（Information Bottleneck）"的循环神经网络，揭示全局工作空间理论"点燃（Ignition）"现象背后的计算机制，探索大脑如何从时序信息中自发涌现出抽象的几何规则。

---

## 目录

1. [实验范式](#1-实验范式八边形几何序列任务)
2. [所有已确认的参数](#2-所有已确认的参数一览表)
3. [模型架构](#3-模型架构)
4. [损失函数](#4-损失函数)
5. [实验工作流](#5-实验工作流)
6. [监控指标](#6-监控指标)
7. [分析方法](#7-分析方法)
8. [预期结果图](#8-预期结果图)
9. [待确认的技术细节](#9-待确认的技术细节)
10. [代码实现顺序](#10-代码实现顺序)

---

## 1. 实验范式：八边形几何序列任务

> 复刻 Stanislas Dehaene (2022) 经典范式，加入计算视角。

### 1.1 输入空间

- 一个正八边形的 **8 个顶点**，编号 $k \in \{0, 1, ..., 7\}$
- **输入表示（已确认）**：使用几何坐标，保留欧氏几何结构：

$$\text{input}_k = \left[\cos\left(\frac{2\pi k}{8}\right),\ \sin\left(\frac{2\pi k}{8}\right)\right] \in \mathbb{R}^2$$

> **为什么不用 One-hot**：One-hot（8维）丢失了欧氏几何结构，模型只能学习序号规律；几何坐标（2维）保留了顶点间的真实距离关系，是真正"涌现几何规则"的必要前提。消融实验中可对比 one-hot，本身即为一个独立结果。
>
> **优雅的对称性**：输入是 2D，瓶颈层也是 2D（见第3节），这意味着模型将把 d_model 维的内部状态压缩回与原始输入等维的空间——它必须"重新发明"输入几何。

### 1.2 预测任务（已确认）

**自回归预测（Autoregressive Next-Step Prediction）**：

- 给定序列 $x_1, x_2, ..., x_t$，预测下一个顶点 $x_{t+1}$
- 输出：8 分类（Softmax over 8 vertices）
- **随机基线准确率**：$\frac{1}{8} = 12.5\%$

### 1.3 序列类型（三种难度梯度）

| 类型 | 规则 | 生成公式 | 示例（从顶点 0 开始） | 规则复杂度 |
|------|------|----------|----------------------|-----------|
| **简单旋转（Simple）** | 顺时针 +1 步 | $x_{t+1} = (x_t + 1) \bmod 8$ | 0→1→2→3→4→5→6→7→0... | 极低 |
| **层级嵌套（Nested）** | 进二退一（+2,-1 交替） | 奇步 +2，偶步 -1（均 mod 8） | 0→2→1→3→2→4→3→5→4→6... | 中高 |
| **随机对照（Random）** | 无规律，均匀采样 | $x_{t+1} \sim U\{0,...,7\}$ | 0→5→2→7→1→4→6→3→0... | 不可压缩 |

**嵌套序列的数学性质（重要）**：

- 微观结构（Micro-structure）：每 2 步构成一个子程序 (+2, -1)，净移动 +1
- 八边形上的完整周期：16 步（绕回原点）
- L=10 覆盖 **5 个完整微周期**：足够确认规律，但看不到全貌，逼迫模型真正泛化而非记忆

> **Random 序列**是至关重要的对照组——模型在此类序列上的准确率应**永远保持在 12.5%**，否则说明模型在过拟合。

---

## 2. 所有已确认的参数（一览表）

| 参数名 | 确认值 | 理由 |
|--------|--------|------|
| **序列类型** | Simple (+1) / Nested (+2,-1) / Random | 候选C，最能体现程序归纳 |
| **输入编码** | 几何坐标 [cos(2πk/8), sin(2πk/8)] | 保留欧氏结构，2D输入 |
| **输入维度** | 2（几何坐标） | 不是 8（one-hot 已排除） |
| **序列长度 L** | 10 | 覆盖 5 个微周期，足够确认规律 |
| **循环次数 T** | 8 | 对应 ~300ms 神经传导（50ms/步） |
| **d_model** | 32 | 嵌入维度，2 → 32 |
| **瓶颈维度** | 2（或 4，待消融） | 极度压缩，逼迫只记"规则" |
| **VIB 应用范围** | 全序列状态 [B, L, d_model]（方案A） | 见第9节确认 |
| **β 扫描列表** | {0, 0.01, 0.1, 1.0} | β=0 为必须对照组 |
| **β 调度** | KL 退火，前 20% steps 从 0 升至 β_max | 避免训练初期信息被截断 |
| **预测任务** | 自回归下一顶点（8分类） | 随机基线 12.5% |
| **T=1 基线** | 配合 Causal Mask 限制注意力 | 等价"无意识/局部"处理 |

---

## 3. 模型架构

### 3.1 Tensor 形状全流程

```
输入序列: L 个顶点的几何坐标
Shape: [B, L=10, 2]
     │
     ▼ 线性投影（Embedding）
Shape: [B, L=10, d_model=32]
     │
     ▼ 加入位置编码（Sinusoidal，标记序列中的时间位置）
Shape: [B, L=10, d_model=32]  ← H_0
     │
     ┌──────────────────────────────────────────┐
     │  循环展开 T=8 次（权重共享）              │
     │                                          │
     │  for t in 1..8:                          │
     │    H_full = TransformerLayer(H_{t-1})    │  ← 同一组权重 W
     │    Shape: [B, L=10, d_model=32]          │
     │                                          │
     │    μ, σ = Linear(H_full)                 │  ← VIB Encoder
     │    Shape: [B, L=10, bottleneck=2]        │
     │    Z_t = μ + ε·σ,  ε~N(0,I)             │  ← 重参数化
     │    H_t = Linear(Z_t)                     │  ← 投影回 d_model
     │    Shape: [B, L=10, d_model=32]          │
     │                                          │
     │    记录: RCR_t = ‖H_t - H_{t-1}‖        │  ← 循环收敛速度
     │    记录: KL_t = KL(N(μ,σ²)‖N(0,I))      │  ← 信息压缩量
     │  end                                     │
     └──────────────────────────────────────────┘
     │
     ▼ 取最后一个序列位置的隐状态 H_T[:, -1, :]
Shape: [B, d_model=32]
     │
     ▼ 输出头（Output Head）
Shape: [B, 8]  → Softmax → 下一顶点的概率分布
```

### 3.2 变分信息瓶颈（VIB）数学细节

**重参数化（Reparameterization Trick）**：

$$\mu_t,\ \log\sigma_t^2 = W_\mu \cdot H_t,\ W_\sigma \cdot H_t$$

$$Z_t = \mu_t + \epsilon \cdot \sigma_t, \quad \epsilon \sim \mathcal{N}(0, I)$$

$$H_t = W_{\text{back}} \cdot Z_t \quad \text{（投影回 d\_model）}$$

**信息瓶颈惩罚项（解析解）**：

$$\text{KL}\left(\mathcal{N}(\mu_t, \sigma_t^2) \| \mathcal{N}(0, I)\right) = \frac{1}{2}\sum_j \left(\mu_{t,j}^2 + \sigma_{t,j}^2 - \log\sigma_{t,j}^2 - 1\right)$$

**物理意义**：这是"遗忘门"——每一个循环步，都强迫网络把当前 32 维的内部状态压缩进 2 维的瓶颈。在 2D 空间里，模型**无法同时存储**当前位置（需要 ~3 bits）和规则（+2/-1，需要 1 bit）。随着 β 增大，它被迫选择——而规则的信息熵远低于位置，所以最终只能保留规则。

---

## 4. 损失函数

### 4.1 完整损失

$$\mathcal{L} = \underbrace{-\log P(Y \mid Z_T)}_{\text{预测误差（交叉熵）}} + \beta(t) \cdot \underbrace{\frac{1}{T}\sum_{t=1}^{T}\text{KL}\left(\mathcal{N}(\mu_t, \sigma_t^2) \| \mathcal{N}(0, I)\right)}_{\text{所有循环步的平均信息瓶颈代价}}$$

> **为什么对所有循环步取平均**：不只是压缩最终状态，而是在**每一步循环**都施加压力，才能模拟"思考过程中的持续遗忘"。

### 4.2 β 退火调度（KL Annealing）

$$\beta(t) = \beta_{\max} \cdot \tanh\left(\frac{t}{T_{\text{warmup}}}\right), \quad T_{\text{warmup}} = 0.2 \times T_{\text{total}}$$

| β_max | 阶段 | 行为预期 |
|-------|------|----------|
| 0 | 对照组（无瓶颈） | 学得快，但表征混乱，无 Ignition |
| 0.01 | 弱压缩 | 轻微压缩，可能出现弱 Ignition |
| 0.1 | 中压缩 | **预期出现最清晰的 Ignition** |
| 1.0 | 强压缩 | 压缩强，Ignition 或快或不出现 |

> **β=0 是最重要的对照**：它证明"是瓶颈让表征坍缩成规则的"，而不是循环本身。

---

## 5. 实验工作流

### 阶段 0：数据生成

```
对每种序列类型，生成 N=10,000 条序列：
  ├── 序列长度: L=10 个顶点（输入 x_1...x_10）
  ├── 标签: 对每个时间步 t，标签是 x_{t+1}（共 9 个预测任务）
  └── 输入编码: 几何坐标 [cos(2πk/8), sin(2πk/8)] ∈ ℝ²

数据集划分:
  Train: 70% | Val: 15% | Test: 15%
  ★ 三类序列分别划分，确保比例均衡
```

### 阶段 1：T=1 基线（"无意识"处理）

```
配置:
  T=1, Causal Mask（每步只能看到当前及之前位置）, β=0

验证目标:
  Simple / Nested / Random 三类序列准确率均 ≈ 12.5%

若 Simple 已经能学会 → 说明 T=1 仍泄漏信息，需收紧 Mask
意义: 模拟 Dehaene 定义的"掩蔽/无意识处理"
```

### 阶段 2：2×2 消融实验（核心对照）

|  | **有循环（T=8）** | **无循环（T=1, Causal Mask）** |
|---|---|---|
| **有 VIB（β>0，退火）** | ★ **主模型** | 消融 A |
| **无 VIB（β=0）** | 消融 B | 消融 C（= 阶段1） |

**预期结论**：

- **主模型**：Simple 和 Nested 出现"点燃"，三指标同步突变
- **消融 B**（循环无瓶颈）：可能学会规则，但**无表征坍缩**，KL 不突变
- **消融 A**（瓶颈无循环）：无法学会复杂嵌套规则，准确率停留在 12.5%
- **消融 C**：所有序列 12.5%（确认基线）

**核心论点**：循环与瓶颈对于"点燃"现象**缺一不可**。

### 阶段 3："点燃"监控训练

```
训练配置:
  优化器: AdamW, lr=1e-3, weight_decay=1e-4
  总步数: 2000 steps
  Batch size: 128

每 100 steps 在验证集执行一次快照（Snapshot）:
  记录 Accuracy（三类分别）
  记录 KL 散度（平均值 + 分布）
  记录 RCR（每个循环步 t 的 ‖H_t - H_{t-1}‖）
  记录 线性探针准确率（位置探针 P + 规则探针 R）
```

### 阶段 4：机制深度分析

```
训练结束后执行:
  1. 线性探针全流程分析（见 7.1）
  2. 泛化测试到新规则（见 7.2）
  3. 瓶颈表示 PCA 可视化（见 7.3）
  4. β 扫描对比图
```

---

## 6. 监控指标

### 6.1 行为指标：预测准确率

$$\text{Accuracy}_{\text{type}} = \frac{\text{正确预测的下一顶点数}}{\text{总预测数}} \times 100\%$$

| 序列类型 | 顿悟前 | 顿悟后 |
|----------|--------|--------|
| Simple | 12.5% | ~100% |
| Nested | 12.5% | ~100% |
| **Random** | **12.5%（永远）** | **12.5%（永远）** |

### 6.2 动力学指标：循环收敛速度（RCR）

> **术语说明**：此指标本质是不动点分析（Fixed-point dynamics），而非 Lyapunov 指数（后者衡量对初始条件的敏感性）。正式命名为**"循环收敛速度（Recurrence Convergence Rate）"**。

$$\text{RCR}_t = \|H_t - H_{t-1}\|_2, \quad t = 2, ..., T$$

| 阶段 | 预期行为 |
|------|----------|
| 顿悟前 | RCR 各步波动大（网络在犹豫，状态不稳定） |
| **顿悟瞬间** | RCR **突然归零**（状态锁定，进入稳定吸引子） |
| 顿悟后 | RCR ≈ 0（驻波/定态模式） |

### 6.3 信息指标：瓶颈处 KL 散度

$$\text{KL}_t = \text{KL}\left(\mathcal{N}(\mu_t, \sigma_t^2) \| \mathcal{N}(0, I)\right)$$

| 阶段 | 预期行为 | 解读 |
|------|----------|------|
| 顿悟前 | KL 值高 | 瓶颈里塞满杂乱坐标（高熵） |
| **顿悟瞬间** | KL 值**断崖式下跌** | 遗忘坐标，只保留规则 |
| 顿悟后 | KL 值稳定在低水平 | "规则"信息量 ≪ "坐标"信息量 |

> **三个指标同时突变的那一刻 = 计算意义上的"点燃（Ignition）"。**

---

## 7. 分析方法

### 7.1 线性探针（Linear Probing）

在每个循环步 $t = 1, ..., 8$ 的隐状态 $H_t$（取所有序列位置的平均）上，分别训练两个**线性**分类器：

| 探针 | 预测目标 | 类别数 | 意义 |
|------|----------|--------|------|
| 探针 P（Position） | 当前顶点位置 $k$ | 8 | 模型还记得"在哪里"吗？ |
| 探针 R（Rule） | 当前序列规则类型 | 3 | 模型知道"在执行什么规则"吗？ |

**预期时间轨迹**：

```
训练步数 ──────────────────────────────────►

探针P准确率（位置）:
  ████████████████▓▓▓▓▓░░░░░░░░    ← 顿悟后下降
  (模型不再记坐标)

探针R准确率（规则）:
  ░░░░░░░░░░░░░░░░▓▓▓▓▓████████    ← 顿悟后上升
  (模型开始记规则)
                  ↑
               顿悟点（三指标同步突变处）
```

### 7.2 泛化测试（Generalization Test）

训练完成后，在**从未见过的新规则**上零样本测试：

| 测试规则 | 说明 | 预期（若真正抽象） |
|----------|------|-------------------|
| $+3$ 步跳跃 | 完全新规则 | 快速适应（few-shot） |
| $-2$ 步跳跃 | 训练时未见 | 快速适应 |
| $+1$ 后 $+2$ 交替 | 新嵌套变体 | 部分迁移 |

**结论标准**：
- 泛化成功 → 模型真正学会了"加法算符"的抽象，瓶颈存储了可迁移的规则表征
- 泛化失败 → 模型只是记住了训练序列的统计模式

### 7.3 表征几何可视化（PCA + RSA）

将瓶颈向量 $Z_T \in \mathbb{R}^2$（已是 2D，无需 PCA）直接散点图可视化：

**预期图像**：

```
顿悟前:                    顿悟后:
  ·  ·  ·                  7 ● ─── ● 0
 · ·  ·  ·                  |         |
 · · ·  ·   →              6 ●         ● 1
  ·  ·  ·                  |         |
  ·  ·                     5 ● ─── ● 2
（随机散布）                  4    ●    3
                          （重现八边形几何！）
```

可用表征相似性分析（RSA）定量计算内部几何与真实八边形的对齐程度。

---

## 8. 预期结果图

### 主图：三指标同步突变（规则序列 vs. 随机对照）

```
纵轴（左）: 预测准确率 %
纵轴（右）: KL 散度 / RCR（归一化）
横轴: 训练步数

          ← Simple / Nested 序列上 →        ← Random 序列（永远12.5%）→

100% ───────────────────╮──────────────    12.5% ─────────────────────────
                        │╲  ← 准确率（红线）
 50% ───────────────────┤ ─────────────
                        │
12.5% ──────────────────╯

KL  ────────────────────╮
High                    │╲   ← KL 散度（蓝线）
                        │ ╲
Low ────────────────────╯  ─────────────

RCR ──────────────────────╮
High                       │
                           │╲   ← 循环收敛速度（绿线）
≈0  ───────────────────────╯ ─────────────

                           ↑
                     Step ~500 (顿悟点)
                  ★ 三线在此处同时突变 ★
```

### 辅图 B：2×2 消融对比（β=0 无 Ignition，β>0 有 Ignition）

### 辅图 C：线性探针轨迹（Position vs. Rule 解码性的交叉）

### 辅图 D：瓶颈 2D 散点图（顿悟前混沌 → 顿悟后八边形）

---

## 9. 所有设计决策（已全部确认）

| 决策项 | 确认值 | 备注 |
|--------|--------|------|
| VIB 应用范围 | **方案 A**：对全序列 `[B, L, d_model]` 施加 VIB | 每步循环都被过滤，模拟"持续遗忘" |
| Bottleneck 维度 | **dim=2** | 与几何坐标等维，模型必须"重新发明"输入几何 |

**所有参数已确认完毕，可以开始写代码。**

---

## 10. 代码实现顺序

```
Step 1: data_generator.py
  ├── generate_simple(n, L)       # +1 规则序列
  ├── generate_nested(n, L)       # +2,-1 规则序列
  ├── generate_random(n, L)       # 均匀随机序列
  └── encode_geometric(k)         # [cos(2πk/8), sin(2πk/8)]

Step 2: model.py
  ├── VIBLayer                    # μ/σ 输出 + 重参数化 + 投影回 d_model
  ├── RecurrentTransformer        # 权重共享循环，内嵌 VIBLayer
  └── forward() → 返回 (logits, all_KL, all_RCR)

Step 3: train.py
  ├── β 退火调度器
  ├── 训练循环（带 snapshot 每 100 steps）
  └── 保存 checkpoint + metrics.json

Step 4: monitor.py
  ├── 实时绘制三指标曲线
  └── 标注"点燃点"（三指标同步突变处）

Step 5: analysis.py
  ├── LinearProbe(H_t, target='position'/'rule')
  ├── generalization_test(model, new_rules)
  └── plot_bottleneck_2d(Z_T)     # 直接可视化 2D 瓶颈向量

Step 6: ablation.py
  └── 批量运行 2×2 消融 × β 扫描，汇总对比表格
```

---

*文档版本：v1.2 | 更新日期：2026-02-18 | 更新内容：确认 VIB 方案A（全序列压缩）+ Bottleneck dim=2，所有设计决策已全部锁定，可开始编码*
